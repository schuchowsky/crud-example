import React from "react";

const Title = ({ children }: any) => {
  return (
    <h1
      style={{
        color: "#434275",
      }}
    >
      {children}
    </h1>
  );
};

export default Title;
